name: Build Android APK

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      # 1️⃣ Checkout repository
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Setup Node.js
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 18

      # 3️⃣ Install npm dependencies
      - name: Install dependencies
        run: npm install

      # 4️⃣ Ensure www folder exists
      - name: Ensure www folder
        run: |
          mkdir -p www
          if [ ! -f www/index.html ]; then
            echo "<h1>Hello SkyWatch</h1>" > www/index.html
          fi

      # 5️⃣ Ensure capacitor.config.json exists
      - name: Ensure capacitor.config.json
        run: |
          if [ ! -f capacitor.config.json ]; then
            echo '{
              "appId": "com.skywatch.app",
              "appName": "SkyWatch",
              "webDir": "www",
              "bundledWebRuntime": false
            }' > capacitor.config.json
          fi

      # 6️⃣ Add Android platform if missing
      - name: Add Android platform
        run: |
          if [ ! -d android ]; then
            npx cap add android
          fi

      # 7️⃣ Sync Capacitor
      - name: Sync Capacitor
        run: npx cap sync android

      # 8️⃣ Setup Java
      - name: Setup Java
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: 17

      # 9️⃣ Setup Gradle cache
      - name: Setup Gradle
        uses: gradle/gradle-build-action@v2

      # 🔟 Ensure gradlew is executable
      - name: Make gradlew executable
        working-directory: android
        run: chmod +x gradlew

      # 1️⃣1️⃣ Build Debug APK
      - name: Build Debug APK
        working-directory: android
        run: ./gradlew assembleDebug

      # 1️⃣2️⃣ Upload APK artifact
      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: SkyWatch-debug-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
